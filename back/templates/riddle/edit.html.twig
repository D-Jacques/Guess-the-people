{% extends "base.html.twig" %}

{% block body %}
    <div class="container mx-auto px-4 mt-4">
        <div class="my-2">
            <h1> Gestion des devinettes </h1>
        </div>
        <div class="flex my-3">
            <div class="w-1/2">
                <h2> Editer une devinette </h2>
            </div>

            <div class="flex w-1/2 justify-center">
                <a href="{{path("backoffice_riddle_list")}}">Retour à la liste</a>
            </div>
        </div>
        <div class="flex">
            {# {{ form(form) }} #}
            {{ form_start(form) }}
                {{form_widget(form)}}
                <button type="submit">Créer</button>
            {{ form_end(form)}}
        </div>
    </div>
{% endblock %}